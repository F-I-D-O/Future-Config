---
alwaysApply: false
---
Update @CHANGELOG.md file with the changes in the c++ implementation (cpp folder) between the current version and the previous version. The file is located in the root/cpp directory. The hierarchy of the file is H1: version, H2: sections. The changelog should have these sections:

- `Added` for new features.
- `Changed` for changes in existing functionality.
- `Removed` for now removed features.
- `Fixed` for any bug fixes.

Skip any sections that are not applicable. The current version is in the @vcpkg.json file. To get the changes, use `git diff`. To get the last commit with the previous version, check the @vcpkg.json file til, you find the previous version.